{{!-- <h1>{{title}}</h1> --}}
<header>
  <div class="container">
    <div class="header__inner">
      <h1>Categories</h1>
        <ul>
          {{#each categories}}
          <a href="/src/views/product.hbs">{{this.name}}</a>
          {{/each}}
        </ul>
    </div>
  </div>
</header>