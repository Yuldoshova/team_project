{{!-- <header>
  <div class="container">
    <div class="header__inner">
      <h1>LOGO</h1>
        <ul>
          {{!-- {{categories}} --}}
          {{#each categories}}
            <img src="{{this.image_url}}" alt="img">
            <li>{{this.name}}</li>
          {{/each}}
        </ul>
    </div>
  </div>
</header> -